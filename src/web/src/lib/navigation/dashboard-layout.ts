/**
 * Main column: page content width and gutter (sidebar is separate).
 */
export const dashboardGutterXClass = "px-4 sm:px-6 lg:px-8";

export const dashboardContentMaxClass = "mx-auto w-full max-w-6xl";

/** Horizontal inset from viewport edges for the top header bar (logo, account). */
export const dashboardHeaderEdgeGutterClass = "px-3 sm:px-5 lg:px-6";

/** Vertical padding for the scrollable page area below the header. */
export const dashboardPageVerticalClass = "py-8";
